# Environment Variables for Vercel Deployment
# Add these to your Vercel project settings under Environment Variables

# Django Settings
DJANGO_SECRET_KEY=y$x3b0p%r_8u(&^^ksc#5t9xoxda%j^f6xi2%g9s2ifqaer+ij
DJANGO_SETTINGS_MODULE=helpdesk.settings_vercel

# Database Configuration (Use a cloud MySQL service like PlanetScale or Railway)
DB_NAME=django_it_help
DB_USER=your_cloud_db_user
DB_PASSWORD=your_cloud_db_password
DB_HOST=your_cloud_db_host
DB_PORT=3306

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=abbakariamali@gmail.com
EMAIL_HOST_PASSWORD=fjikqzmjqcfqhhfc
DEFAULT_FROM_EMAIL=superdoll.co.tz

# Note: You'll need to set up a cloud MySQL database since Vercel doesn't support local databases
# Recommended options:
# - PlanetScale (MySQL-compatible, serverless)
# - Railway (Traditional MySQL hosting)
# - AWS RDS (Enterprise-grade MySQL)
